# Runtime contracts

The normalized card may omit `interaction`; `CharacterRuntime.interaction` may not. The registry is the single fallback boundary. Deployment secrets, URLs, ports, cache locations, and model credentials remain environment-owned. Pronunciation profile versions identify rewrite/cache behavior and are not GPT-SoVITS parameters.

